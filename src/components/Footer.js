import React from "react";
const Footer =(props)=> {
return(
    
        <footer id={props.id}>
        <div id="footer">   {props.content}  </div>
           
            </footer>

);
}

export default Footer;