import React from "react";

const Main = (props) => {
    return (
        <div id={props.id}>
            <main id="container">This is the main</main>
        </div>
    );
}

export default Main;